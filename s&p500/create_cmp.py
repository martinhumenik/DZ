class Company:
    def __init__(self,  company_id, name, symbol, sector, price, market_cap, pe, pb, ps, ebitda, eps, div_yield):
        self.company_id = company_id
        self.name = name
        self.symbol = symbol
        self.sector = sector
        self.price = price
        self.market_cap = market_cap
        self.pe = pe
        self.pb = pb
        self.ps = ps
        self.ebitda = ebitda
        self.eps =eps
        self.div_yield = div_yield
